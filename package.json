{
  "name": "chromecast_stream",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "sync-images": "./sync-images.sh",
    "build-deploy": "npm run build && npm run deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^7.1.0",
    "vite-plugin-static-copy": "^3.1.1"
  }
}
